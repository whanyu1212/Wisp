---
title: Releasing
---

# Releasing

TODO — document the release process.

Cover:

- Version bumping in `pyproject.toml`.
- Updating `CHANGELOG.md`.
- The `release.yml` workflow and what triggers it.
- Publishing to PyPI as `wisp-ai`.
