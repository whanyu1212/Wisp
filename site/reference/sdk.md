---
title: SDK
---

# SDK

TODO — adapt from `README.md` § SDK.

Cover:

- Constructing a `WispConfig` and driving a session in-process.
- The typed command surface, shared with the JSONL RPC host.
- Consuming events.
- Embedder responsibilities around trust and protected paths.
