---
title: RPC protocol
---

# RPC protocol

TODO — adapt from the RPC/JSON schema sections of `README.md`.

Cover:

- Transport: newline-delimited JSON on stdin/stdout, started with `wisp --mode rpc`.
- Every command, with its request and response shape.
- The approval round-trip.
- Emitted events and how they relate to [Events](./events).
